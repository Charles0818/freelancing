import React from 'react';
import { View, Text } from 'react-native';
import { Container, Section } from '../Container';
import { useFormInput } from '../../helpers/index';
import { Form } from '../../components';
import { styles, colors } from '../styles';

const ConfirmEmail = () => {
    return (
        <Container>
            
        </Container>
    )
}

export default ConfirmEmail;

// function TheResult() {
//     var value = document.getElementById("displayHere").value;

//     value = parseInt(value);
//     var output = document.getElementById("Grade");

//     if(value <= 39 ) {
//         output.value = "FAILED"
//     }
//     if((value > 39) && (value<=44)) {
//         output.value = "E"
//     }
//     if((value > 44) && (value <= 49)) {
//         output.value = "D"
//     }

//     // AND SO ON...
// }


